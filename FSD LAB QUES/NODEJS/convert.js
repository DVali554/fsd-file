// Import the package -npm install uppercase

const upperCase = require("uppercase");

// Original string
const myString = "hello, node.js";

// Convert to uppercase
const upperString = upperCase(myString);

// Display the result
console.log("Uppercase:", upperString);
